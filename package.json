{
  "name": "backend_dictionary_api",
  "version": "1.0.0",
  "description": "dictionary api created using nodejs so the api key will not be reaval to the client side",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "anmol dhanda",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "express": "^4.18.3"
  }
}
